# sov3-topology

> SOV3 protocol repository — part of the MEOK sovereign grid.

## Purpose

 echo "Layer-0 BFT substrate audit tap. Provides passive audit logging and optional active veto for tool invocations across the SOV3 empire." ;;
  sov3-topology) echo "Canonical topology registry for domains, hives, tools, cross-links, and protocol layers rendered by the MEOK Grid." ;;
  meok-grid) echo "Deployment and governance wrapper for the MEOK Grid sovereign interface." ;;
  gamemcp) echo "Game MCP bridge: standardised Model Context Protocol gateway for game-state and NPC-tool interactions." ;;
  hermes-integration) echo "Hermes integration bridge for cross-terminal orchestration, handoff, and memory sync." ;;
  minimax-bridge) echo "Bridge to the MiniMax service layer, exposing its capabilities via the SOV3 tool gateway." ;;
esac)

## Status

- Seed scaffold — README + repo structure.
- Active development happens in the canonical monorepos (see `Canonical Source`).

## Canonical Source

 echo "- Runtime implementation: `CSOAI-ORG/openmore.ai/meok-one/meok_one/horus_layer0.py`" ;;
  sov3-topology) echo "- TypeScript source: `CSOAI-ORG/meok-ai/meok/ui/src/lib/sov3-topology.ts`" ;;
  meok-grid) echo "- Application source: `CSOAI-ORG/meok-ai/meok/ui`" ;;
  gamemcp) echo "- TBD" ;;
  hermes-integration) echo "- TBD" ;;
  minimax-bridge) echo "- TBD" ;;
esac)

## License

Proprietary — CSOAI / Sovereign Grid.
